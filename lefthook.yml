pre-commit:
    parallel: true
    commands:
        typecheck:
            run: pnpm run typecheck
        lint:
            run: pnpm run lint

commit-msg:
    commands:
        conventional-commit:
            run: npx commitlint --edit $1
